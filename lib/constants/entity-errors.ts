import { EntityErrorInterface } from '../interfaces/entity-error.interface';

/**
 * Constants defining stripe entity errors.
 *
 * @constant
 */
export const EntityErrors: EntityErrorInterface = {
  NOT_FOUND: 'Stripe entity not found.',
};
